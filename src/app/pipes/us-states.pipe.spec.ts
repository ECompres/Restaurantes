import { UsStatesPipe } from './us-states.pipe';

describe('UsStatesPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new UsStatesPipe();
    expect(pipe).toBeTruthy();
  });
});
